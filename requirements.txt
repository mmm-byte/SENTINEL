# ── Core ──────────────────────────────────────────────────────────────────────
google-adk>=0.5.0
pymongo[srv]>=4.6.0
google-cloud-aiplatform>=1.50.0
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
httpx>=0.24.0
fastapi>=0.95.0
uvicorn>=0.22.0
requests>=2.31.0

# ── Track 1: MongoDB (Atlas Vector Search + Change Streams) ───────────────────
motor>=3.3.0

# ── Track 2: Arize Phoenix (tracing + LLM-as-a-Judge evals) ──────────────────
arize-phoenix-otel>=0.6.0
openinference-instrumentation-google-adk>=0.1.0
google-generativeai>=0.8.0

# ── Track 3: Dynatrace (OTel export + custom SENTINEL spans) ─────────────────
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
opentelemetry-api>=1.24.0

# ── Track 4: Fivetran (MCP primary + REST fallback) ───────────────────────────
# requests already included above

# ── Track 5: GitLab (MCP primary + REST fallback + Duo Agent) ─────────────────
# requests already included above

# ── Track 6: Elastic (incident memory + ES|QL search) ────────────────────────
elasticsearch>=8.13.0

# ── Vertex AI Embeddings (MongoDB vector search) ─────────────────────────────
vertexai>=1.0.0
